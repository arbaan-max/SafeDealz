# LIVE BUILD — used automatically by `npm run build`.
# Do not comment these out. Local/live switching for `npm run dev` is in `.env`.
VITE_API_TARGET=live
VITE_LIVE_API_BASE_URL=https://safedealz-production.up.railway.app/api/v1
